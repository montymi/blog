import React from 'react';
import { Box } from '@mui/material';

const Spotlight = () => {
  return <Box sx={{ position: 'relative', maxWidth: 600, margin: 'auto' }}></Box>;
};

export default Spotlight;
